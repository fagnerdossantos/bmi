import 'package:flutter/material.dart';


// COLORS
const Color white = Color.fromRGBO(255, 255, 255, 1);
const Color black = Color.fromRGBO(0, 0, 0, 1);
const Color grey = Color.fromRGBO(158, 158, 158, 1);

// RADIUS
final BorderRadius radiusTwenty = BorderRadius.circular(20);

// Padding
const EdgeInsets paddingTwenty = EdgeInsets.all(20);